<Page
    x:Class="DofGMTool.Views.CharacterManagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:DofGMTool.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:table="using:WinUI.TableView"
    xmlns:wuc="using:WinUICommunity"
    mc:Ignorable="d">
    <Page.Resources>
        <!--  判断字符串是否为空转换为可见性的转换器  -->
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <!--  重量转换为千克的转换器  -->
        <converters:WeightToKgConverter x:Key="WeightToKgConverter" />
        <!--  换行符转换为换行符的转换器  -->
        <converters:NewlineToBreakConverter x:Key="NewlineToBreakConverter" />
        <!--  整数转换为速度的转换器  -->
        <converters:IntSpeedConverter x:Key="IntSpeedConverter" />
        <!--  属性转换为颜色的转换器  -->
        <converters:AttributeToColorConverter x:Key="AttributeToColorConverter" />
        <!--  整数转换为可见性的转换器  -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!--  定义ToolTip的DataTemplate  -->
        <DataTemplate x:Key="ItemToolTipTemplate">
            <ToolTip x:Name="ItemToolTip" Opened="ItemToolTip_Opened">
                <ToolTip.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F4" Invoked="KeyboardAccelerator_Invoked" />
                </ToolTip.KeyboardAccelerators>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Image
                            Width="28"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Source="{Binding BitMap.UriSource.AbsoluteUri}" />
                        <StackPanel Orientation="Horizontal">
                            <!--  这里先随便用个转换器，后面再改成如果是0则设置不可见  -->
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="{Binding ItemRarity.Color}"
                                Visibility="{Binding EquipSlot.EnhancementLevel, Converter={StaticResource IntSpeedConverter}}">
                                <Run Text="+" />
                                <Run Text="{Binding EquipSlot.EnhancementLevel}" />
                            </TextBlock>
                            <TextBlock
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                Foreground="{Binding ItemRarity.Color}">

                                <Run Text="{Binding ItemName}" />
                            </TextBlock>
                        </StackPanel>

                    </StackPanel>
                    <wuc:Divider />
                    <TextBlock
                        HorizontalAlignment="Right"
                        Foreground="{Binding ItemRarity.Color}"
                        Text="{Binding ItemRarity.Name}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Weight, Converter={StaticResource WeightToKgConverter}}" />
                        <StackPanel
                            Grid.Column="1"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <TextBlock Text="{Binding Price}" />
                            <TextBlock>金币</TextBlock>
                        </StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding ItemGroupName}" />
                        <TextBlock
                            Grid.Column="1"
                            HorizontalAlignment="Right"
                            Text="{Binding EquipmentType}" />
                    </Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding UsableJob}" />
                        <TextBlock>可使用</TextBlock>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel
                            Grid.Column="0"
                            Orientation="Horizontal"
                            Visibility="{Binding Durability, Converter={StaticResource StringToVisibilityConverter}}">
                            <TextBlock Text="耐久度 :" />
                            <TextBlock Text="{Binding Durability}" />
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            HorizontalAlignment="Right"
                            Text="{Binding AttachType}" />
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Foreground="Red" Text="Lv" />
                            <TextBlock Foreground="Red" Text="{Binding MinimumLevel}" />
                            <TextBlock Foreground="Red">以上可以使用</TextBlock>
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            HorizontalAlignment="Right"
                            Foreground="Gold"
                            Text="最上级" />
                    </Grid>
                    <TextBlock Visibility="{Binding EquipmentPhysicalAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="物理攻击力 +" />
                        <Run Text="{Binding EquipmentPhysicalAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding EquipmentMagicalAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="魔法攻击力 +" />
                        <Run Text="{Binding EquipmentMagicalAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding SeparateAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="独立攻击力 +" />
                        <Run Text="{Binding SeparateAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding EquipmentPhysicalDefense, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="物理防御力 +" />
                        <Run Text="{Binding EquipmentPhysicalDefense}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding EquipmentMagicDefense, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="魔法防御力 +" />
                        <Run Text="{Binding EquipmentMagicDefense}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding MagicalAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="智力 +" />
                        <Run Text="{Binding MagicalAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding PhysicalAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="力量 +" />
                        <Run Text="{Binding PhysicalAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding MagicalDefense, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="精神 +" />
                        <Run Text="{Binding MagicalDefense}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding PhysicalDefense, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="体力 +" />
                        <Run Text="{Binding PhysicalDefense}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding DarkAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Foreground="Purple" Text="暗属性强化" />
                        <Run Text="{Binding DarkAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding WaterAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Foreground="CornflowerBlue" Text="冰属性强化" />
                        <Run Text="{Binding WaterAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding FireAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Foreground="Red" Text="火属性强化" />
                        <Run Text="{Binding FireAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding LightAttack, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Foreground="LightSkyBlue" Text="光属性强化" />
                        <Run Text="{Binding LightAttack}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding PhysicalCriticalHit, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="物理暴击率 +" />
                        <Run Text="{Binding PhysicalCriticalHit}" />
                        <Run Text="%" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding MagicalCriticalHit, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="魔法暴击率 +" />
                        <Run Text="{Binding MagicalCriticalHit}" />
                        <Run Text="%" />
                    </TextBlock>
                    <wuc:Divider />
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding HpMax, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="HP 最大值 +" />
                        <Run Text="{Binding HpMax}" />
                    </TextBlock>
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding MpMax, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="MP 最大值 +" />
                        <Run Text="{Binding MpMax}" />
                    </TextBlock>
                    <TextBlock Visibility="{Binding ElementalProperty, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Foreground="{Binding ElementalProperty, Converter={StaticResource AttributeToColorConverter}}" Text="{Binding ElementalProperty}" />
                        <Run Text="攻击" />
                    </TextBlock>
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding AttackSpeed, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="攻击速度 +" />
                        <Run Text="{Binding AttackSpeed, Converter={StaticResource IntSpeedConverter}}" />
                    </TextBlock>
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding MoveSpeed, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="移动速度 +" />
                        <Run Text="{Binding MoveSpeed, Converter={StaticResource IntSpeedConverter}}" />
                    </TextBlock>
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding CastSpeed, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="施放速度 +" />
                        <Run Text="{Binding CastSpeed, Converter={StaticResource IntSpeedConverter}}" />
                    </TextBlock>
                    <TextBlock Foreground="CornflowerBlue" Visibility="{Binding Stuck, Converter={StaticResource StringToVisibilityConverter}}">
                        <Run Text="命中率减少" />
                        <Run Text="{Binding Stuck}" />
                        <Run Text="%" />
                    </TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <ItemsControl ItemsSource="{Binding Skill}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock>
                                            <Run Text="[" />
                                            <Run Foreground="OrangeRed" Text="{Binding Name}" />
                                            <Run Text="]" />
                                            <Run Text="(" />
                                            <Run Text="{Binding SourceList}" />
                                            <Run Text=")" />
                                            <Run Text="技能等级" />
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding SkillLevelList}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock>
                                            <Run Text="+" />
                                            <Run Foreground="DarkOrange" Text="{Binding LevelUp}" />
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <TextBlock
                        x:Name="ItemDesc"
                        Foreground="CornflowerBlue"
                        Text="{Binding Description, Converter={StaticResource NewlineToBreakConverter}}"
                        TextWrapping="Wrap"
                        Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}" />
                    <TextBlock
                        x:Name="ItemDescDTL"
                        Foreground="CornflowerBlue"
                        Text="{Binding DetailDescription, Converter={StaticResource NewlineToBreakConverter}}"
                        TextWrapping="Wrap"
                        Visibility="Collapsed" />
                    <wuc:Divider Visibility="{Binding FlavorText, Converter={StaticResource StringToVisibilityConverter}}" />
                    <TextBlock
                        Foreground="DarkGray"
                        Text="{Binding FlavorText, Converter={StaticResource NewlineToBreakConverter}}"
                        TextWrapping="Wrap"
                        Visibility="{Binding FlavorText, Converter={StaticResource StringToVisibilityConverter}}" />
                    <TextBlock
                        x:Name="ShowDtl"
                        HorizontalAlignment="Center"
                        Visibility="{Binding DetailDescription, Converter={StaticResource StringToVisibilityConverter}}">
                        查看详细说明(F4)
                    </TextBlock>
                </StackPanel>
            </ToolTip>
        </DataTemplate>
    </Page.Resources>
    <Grid x:Name="ContentArea">
        <Canvas>
            <Image Canvas.ZIndex="0" Source="ms-appx:///Assets/inventory.png" />

            <Image
                x:Name="上衣"
                Canvas.Left="36"
                Canvas.Top="4"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Jacket.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="上衣"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="上衣"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="上衣"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="JacketToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Jacket, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="护腿"
                Canvas.Left="4"
                Canvas.Top="36"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Pants.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="护腿"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="护腿"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="护腿"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="PantsToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Pants, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="腰带"
                Canvas.Left="36"
                Canvas.Top="36"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Belt.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">

                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="腰带"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="腰带"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="腰带"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="BeltToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Belt, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="鞋子"
                Canvas.Left="5"
                Canvas.Top="68"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Shoes.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="鞋子"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="鞋子"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="鞋子"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="ShoesToolTip">

                        <ContentControl Content="{x:Bind ViewModel.Shoes, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>

            <Image
                x:Name="武器"
                Canvas.Left="178"
                Canvas.Top="4"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Weapon.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="武器"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="武器"
                            Text="增幅" />
                        <MenuFlyoutItem Click="ChangeEquipMenuFlyoutItem_Click" Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="WeaponToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Weapon, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>


            <Image
                x:Name="称号"
                Canvas.Left="210"
                Canvas.Top="4"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Title.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="称号"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="TitleToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Title, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="手镯"
                Canvas.Left="178"
                Canvas.Top="36"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Wrist.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="手镯"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="手镯"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="手镯"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="WristToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Wrist, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="项链"
                Canvas.Left="210"
                Canvas.Top="36"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Amulet.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="项链"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="项链"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="项链"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="AmuletToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Amulet, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="戒指"
                Canvas.Left="210"
                Canvas.Top="68"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Ring.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="戒指"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="戒指"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="戒指"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="RingToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Ring, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
            <Image
                x:Name="头肩"
                Canvas.Left="4"
                Canvas.Top="4"
                Width="28"
                Height="28"
                PointerEntered="Image_PointerEntered"
                PointerExited="Image_PointerExited"
                Source="{x:Bind ViewModel.Shoulder.BitMap.UriSource.AbsoluteUri, Mode=OneWay, FallbackValue='ms-appx:///Assets/defaultEquip.png'}">
                <Image.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem
                            Click="PowerupEquipMenuFlyoutItem_Click"
                            Tag="头肩"
                            Text="强化" />
                        <MenuFlyoutItem
                            IsEnabled="False"
                            Tag="头肩"
                            Text="增幅" />
                        <MenuFlyoutItem
                            Click="ChangeEquipMenuFlyoutItem_Click"
                            Tag="头肩"
                            Text="更换装备" />
                    </MenuFlyout>
                </Image.ContextFlyout>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="ShoulderToolTip">
                        <ContentControl Content="{x:Bind ViewModel.Shoulder, Mode=TwoWay}" ContentTemplate="{StaticResource ItemToolTipTemplate}" />
                    </ToolTip>
                </ToolTipService.ToolTip>
            </Image>
        </Canvas>
    </Grid>
</Page>
